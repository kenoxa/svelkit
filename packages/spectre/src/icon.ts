import { define, withPrefix } from './internal'
export const icon = define((options: undefined) => ['icon', withPrefix('icon-', options)])

// #TODO: icon.ts
// accordions, buttons, forms, etc. are all using the .icon class
